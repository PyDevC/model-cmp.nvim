# Memory Management for GPU
> works for ROCm and CUDA version of pytorch
> Running the model Do require GPU right now but will be changed in future to allow cpu usage

## move model to device
moves all model parameters with preserved memory to another device

move_model_to_device_with_memory_preservation(model, target_device, preserved_memory)

- TODO: add more information about utilites
